import React from 'react';
import CommonHandler from '../service/service';

const Allcategory=(props)=>{
       return(
        <CommonHandler childElement={props}/>
       )
   }

export default Allcategory;